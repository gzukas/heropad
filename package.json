{
  "name": "heropad",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"\"",
    "prepare": "husky",
    "api": "pnpm --filter=api",
    "web": "pnpm --filter=web"
  },
  "devDependencies": {
    "eslint": "^9.16.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vite": "6.0.2",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@9.14.4",
  "engines": {
    "node": ">=22.0.0"
  }
}
