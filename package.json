{
  "name": "heropad",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"\"",
    "prepare": "husky",
    "api": "pnpm --filter=api",
    "web": "pnpm --filter=web"
  },
  "devDependencies": {
    "eslint": "^9.15.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "3.3.3",
    "turbo": "^2.3.0",
    "typescript": "^5.6.3",
    "vite": "5.4.11",
    "vitest": "^2.1.5"
  },
  "packageManager": "pnpm@9.13.2",
  "engines": {
    "node": ">=22.0.0"
  }
}
